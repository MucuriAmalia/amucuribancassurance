version: "3.8"

services:

  recipe:
    container_name: broker
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8084:8080'
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://DESKTOP-LV9KCAC:5432/absa_insurance
      - SPRING_DATASOURCE_USERNAME=peter
      - SPRING_DATASOURCE_PASSWORD=manu
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    depends_on:
      - psql-db
